const drawBoard= require('./drawBoard');

test("Test if the first row prints", ()	=> {
	expect(Add("1,2,3")).toBe("1, 2, 3");
});

test("Test if the second row prints", ()	=> {
	expect(Add("1,2,3")).toBe("1, 2, 3");
});